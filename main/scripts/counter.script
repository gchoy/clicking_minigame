function on_message(self, message_id, message, sender)
	if message_id == hash("start") then
		print("message to counter")
		print(message.count)
		label.set_text("#counter_label", "Coins:" .. message.count )
	end
end

function update(self, dt)
	
	
end